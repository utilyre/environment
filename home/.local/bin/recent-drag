#!/bin/sh

# TODO: report segfault to bemenu devs (happens with -l'8 up', especially when -f is also specified)
files="$(recent | bemenu -fl8 -pDrag)" || exit 1
printf '%s\n' "$files" | dragon-drop -a -x -T -I -s 48
