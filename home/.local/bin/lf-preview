#!/bin/sh
set -e

mkdir -p -- "$LF_CACHE/thumbnails"

# delete all but the 100 newest thumbnails
find "$LF_CACHE/thumbnails" -mindepth 1 -maxdepth 1 -type f -printf '%T@\t%p\n' |
	sort -n |
	head -n-100 |
	cut -f2 |
	xargs -d'\n' rm -f

mime="$(file -b --mime-type -- "$1")"

# genthumb <input> <output>
genthumb() {
	case "$mime" in
		application/pdf) magick -density 72 -background white "$1[0]" -resize 256x -flatten "$2" ;;
		*) ffmpeg -i "$1" -frames:v 1 -filter:v scale=256:-1 -- "$2" ;;
	esac
}

case "$mime" in
	application/pdf | audio/* | image/* | video/*)
		thumbnail="$LF_CACHE/thumbnails/$(printf '%s:%s' "$1" "$(stat -c%Y -- "$1")" | base64 -w0).png"
		if [ ! -f "$thumbnail" ] && ! genthumb "$1" "$thumbnail"
		then
			file -b -- "$1" | fold -sw "$2"
			exit 1
		fi

		chafa --polite=on --bg=black -t 1 -f sixel -s "$((16 < $2 ? 16 : $2))x" -- "$thumbnail"
		;;
	text/*) cat -- "$1" | fold -sw "$2" ;;
	*) file -b -- "$1" | fold -sw "$2" ;;
esac
